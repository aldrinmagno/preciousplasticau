# Mailchimp Configuration
# Get these values from your Mailchimp account:
# 1. Go to Account → Extras → API keys to get your API key
# 2. Go to Audience → Settings → Audience name and defaults to get your List ID
# 3. The server prefix is the part after the dash in your API key (e.g., if key ends with -us11, use us11)

MAILCHIMP_API_KEY=your_mailchimp_api_key_here
MAILCHIMP_LIST_ID=your_mailchimp_list_id_here
MAILCHIMP_SERVER_PREFIX=your_server_prefix_here